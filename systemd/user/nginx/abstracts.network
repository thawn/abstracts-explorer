# Shared Podman network for all Abstracts Explorer containers.
# All containers that reference this file communicate over this internal bridge.
# No ports on this network are exposed to the host directly; ingress is handled
# by the nginx container which publishes only 127.0.0.1:8080 and 127.0.0.1:8443.

[Network]
Driver=bridge
